CREATE TABLE IF NOT EXISTS linkUserInterest (
  id_geek int NOT NULL,
  id_ci int NOT NULL,
);

ALTER TABLE linkUserInterest
  ADD FOREIGN KEY (id_geek) REFERENCES geek(id);

ALTER TABLE linkUserInterest
  ADD FOREIGN KEY (id_ci) REFERENCES centreInteret(id);